// DISPOSABLE LINT FIXTURE (TASK-009 / SPEC-005).
// Exists only to exercise the oxlint boundaries/dependencies rule set at
// .oxlintrc.json. Not a real feature module -- safe to delete once the
// boundary-enforcement setup is proven and merged.
export const fixtureA = "fixture-a";
